(program
  (let
    (nonrec)
    (datatypebind
      (datatype (tyvardecl Unit (type))  Unit_match (vardecl Unit Unit))
    )
    (lam
      ds
      (con string)
      (let
        (nonrec)
        (termbind (strict) (vardecl b (con unit)) [ (builtin trace) ds ])
        Unit
      )
    )
  )
)